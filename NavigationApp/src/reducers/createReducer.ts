const state = {
    
}

export default (state = 0, action) => {
    switch (action.type) {
        case 'SAVE_DESCRIPTION':
            return {

            }

        case 'SELECT_TYPE':
            return {

            }
        
        case 'CREATE_TODO':
            return {

            }
    }
}